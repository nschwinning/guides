## Installation

```sh
brew install mongodb-atlas
```

## Usage

Connect to Cluster:

```sh
atlas auth login
```

List Clusters:

```sh
atlas clusters list
```

Load Sample Data:

```sh
atlas clusters loadSampleData Cluster0 sample_restaurants.restaurants
```